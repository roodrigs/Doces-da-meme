version: '3.9'
 
services:
  backend:
    build: .
    container_name: backend-app
    restart: unless-stopped
    env_file: .env
    ports:
      - '3012:3000'
    networks:
      - backend-net
    volumes:
      - .:/app
      - /app/node_modules
 
networks:
  backend-net:
    driver: bridge